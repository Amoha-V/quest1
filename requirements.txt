# ---- core pipeline ----
yt-dlp>=2024.8.6
static-ffmpeg>=2.7   # bundles ffmpeg/ffprobe binaries via pip, sidesteps system PATH issues
opencv-python-headless>=4.9.0
numpy>=1.26.0
rapidfuzz>=3.9.0          # fuzzy text similarity for dialogue matching
python-dotenv>=1.0.1
pydantic>=2.7.0

# ---- OCR engine ----
# easyocr ships a DBNet detector + CRNN-style recognizer out of the box,
# which is what core/ocr/engine.py wraps by default. Swappable later.
easyocr>=1.7.1
torch>=2.2.0
torchvision>=0.17.0

# ---- ASR engine (spoken dialogue) ----
# faster-whisper: CTranslate2-based Whisper reimplementation, CPU-efficient.
# Runs concurrently with OCR (core/combined_resolver.py) since target text
# may be spoken rather than shown as on-screen text.
faster-whisper>=1.0.0

# ---- service layer (optional, only needed if running FastAPI app) ----
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
redis>=5.0.4
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
minio>=7.2.7

# ---- tests ----
pytest>=8.2.0
